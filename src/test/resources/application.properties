spring.application.name=ewallet
spring.profiles.active=test
server.port=8080

mock.server.basePath=http://localhost:8080

# db config
spring.datasource.url=jdbc:postgresql://localhost:5432/ewalletdb
spring.datasource.username=admin
spring.datasource.password=P@ssw0rd
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# External User API Configuration
ewallet.external.user.basePath=${mock.server.basePath}
ewallet.external.user.getUsers=/users

# RestTemplate Configuration
ewallet.resttemplate.connectTimeout=5000
ewallet.resttemplate.readTimeout=5000
